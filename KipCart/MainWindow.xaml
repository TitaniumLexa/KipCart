<Window x:Class="KipCart.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KipCart"
        mc:Ignorable="d"
        Title="Корзиночка" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ContentControl Grid.Column="0" Content="{Binding GoodsView}" Margin="2"/>
        <ContentControl Grid.Column="1" Content="{Binding PurchaseView}" Margin="2"/>
        <ContentControl Grid.Column="2" Content="{Binding PurchaseHistoryView}" Margin="2"/>
    </Grid>
</Window>
